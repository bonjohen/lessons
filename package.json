{
  "name": "lessons-hub",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "harvest": "python scripts/harvest_lessons.py",
    "validate:lessons": "python scripts/validate_lessons.py",
    "corpus": "python scripts/build_rag_corpus.py",
    "validate:corpus": "python scripts/validate_rag_corpus.py",
    "embed": "python scripts/embed_rag_corpus.py",
    "backend": "cd backend && uvicorn app.main:app --reload",
    "index": "npx pagefind --site dist",
    "build:full": "npm run harvest && npm run validate:lessons && npm run corpus && npm run validate:corpus && npm run build && npm run index",
    "test:e2e": "npx playwright test --config tests/e2e/playwright.config.ts",
    "test:e2e:smoke": "npx playwright test --config tests/e2e/playwright.config.ts smoke.spec.ts",
    "test:e2e:links": "npx playwright test --config tests/e2e/playwright.config.ts link-crawl.spec.ts"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "astro": "^6.3.1",
    "marked": "^18.0.3",
    "sanitize-html": "^2.17.3",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/sanitize-html": "^2.16.1",
    "pagefind": "^1.5.2"
  }
}
